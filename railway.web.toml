# Servicio GiDi WEB → variable RAILWAY_CONFIG_FILE=railway.web.toml

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "pnpm --filter web start"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
